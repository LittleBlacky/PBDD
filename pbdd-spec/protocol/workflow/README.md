# Workflow Protocol

Workflow definitions are state machines. They describe how a runtime moves a
project from intent to durable engineering output.

Markdown explains workflow intent. YAML files in `workflow/` are the machine
readable definitions.

