# Lifecycle Protocol

Lifecycle documents define valid states for first-class PBDD entities.

The lifecycle is normative. A runtime must reject or repair invalid transitions
instead of silently writing inconsistent brain state.

